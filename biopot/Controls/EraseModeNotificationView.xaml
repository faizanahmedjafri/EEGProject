<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:localization="clr-namespace:biopot.Resources.Strings;assembly=biopot"
             x:Class="biopot.Controls.EraseModeNotificationView">
  <ContentView.Content>
      <StackLayout Orientation="Vertical" BackgroundColor="{StaticResource errorColor}" Padding="4">
         <Label Text="{x:Static localization:Strings.EraseModeNotificationTitle}"
                HorizontalOptions="Center"
                Style="{StaticResource EraseModeTitleStyle}"/>

            <Label Text="{x:Static localization:Strings.EraseModeNotificationSubtitle}"
                   HorizontalOptions="Center"
                   Style="{StaticResource EraseModeSubtitleStyle}"/>
        </StackLayout>
  </ContentView.Content>
</ContentView>